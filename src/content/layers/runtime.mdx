---
slug: runtime
title: Runtime
short_description: Inference engines that serve tokens from weights.
tier: core
order: 6
lock_in_vector: runtime
sovereignty_relevance: 5
related_layers: [silicon, weights, agents]
updated: 2026-05-13
---

How weights become inference. The engines that take a transformer model
and serve tokens efficiently on actual hardware. Runtime is the layer
most invisible to end users but most decisive for production economics:
the same model can cost ten times more on a poorly-tuned runtime than a
well-tuned one. This is also the layer where the open ecosystem has done
its most surprising work. vLLM (PagedAttention, production-grade),
SGLang (RadixAttention, structured generation), and llama.cpp
(local-first, GGUF) are open projects that repeatedly catch up to or
beat closed equivalents like TensorRT-LLM, often within months. When the
workload is well-defined, open runtime wins on technical merits even
when closed runtime still dominates hyperscaler revenue.
